name: Sync releases (DEPRECATED)

# Deprecated: artifact publishing now happens inside the orchestrator when
# artifacts_download is purchased.
on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is deprecated. Artifact publishing now happens inside the orchestrator when artifacts_download is purchased."
          echo "You can safely delete .github/workflows/sync-releases.yml."
