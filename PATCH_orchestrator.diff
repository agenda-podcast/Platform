*** Begin Patch
*** Update File: platform/orchestration/orchestrator.py
@@
-    billing.validate_minimal(
-        billing_state_dir,
-    )
+    # Fresh-start support:
+    # If billing-state release/assets were deleted, bootstrap local billing-state from
+    # the repo template (releases/billing-state-v1/) and (best-effort) republish to GitHub Releases.
+    try:
+        billing.validate_minimal(
+            billing_state_dir,
+        )
+    except FileNotFoundError:
+        from platform.billing.bootstrap import bootstrap_billing_state_if_missing
+        bootstrap_billing_state_if_missing(
+            billing_state_dir,
+            repo_root=os.getcwd(),
+            allow_publish=True,
+        )
+        billing.validate_minimal(
+            billing_state_dir,
+        )
*** End Patch
