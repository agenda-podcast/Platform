module_id: deliver_email
name: deliver_email
version: 0.2.1
kind: delivery
description: "System delivery module: send package.zip via email (SMTP attachment)."

metadata:
  max_package_bytes: 20866662

requirements:
  secrets:
    - name: EMAIL_SMTP_HOST
      note: "SMTP host for email delivery (required)."
    - name: EMAIL_SMTP_PORT
      note: "SMTP port for email delivery (required)."
    - name: EMAIL_SMTP_USERNAME
      note: "SMTP username (optional; if unset, no SMTP AUTH is performed)."
    - name: EMAIL_SMTP_PASSWORD
      note: "SMTP password (required if EMAIL_SMTP_USERNAME is set)."
    - name: EMAIL_FROM_EMAIL
      note: "Sender email address (required)."
  vars:
    - name: EMAIL_SMTP_USE_TLS
      note: "Use STARTTLS when connecting to SMTP. Default true."
  inputs:
    port:
      - id: package_zip
        type: file
        required: true
        format: application/zip
        description: "ZIP file to deliver."
      - id: manifest_json
        type: file
        required: false
        format: application/json
        description: "Optional manifest for the delivered package."
      - id: recipient_email
        type: string
        required: true
        description: "Recipient email address."
      - id: subject
        type: string
        required: false
        description: "Email subject (optional)."
      - id: body
        type: string
        required: false
        description: "Email body (optional)."
    limited_port: []
  outputs:
    port:
      - id: delivery_receipt_json
        type: file
        format: application/json
        description: "Delivery receipt (status, message, timestamps)."
      - id: delivery_log_json
        type: file
        format: application/json
        description: "Detailed, auditable delivery log (attempt metadata, config checks, result)."
      - id: report_json
        type: file
        format: application/json
        description: "Machine-readable failure report (for orchestrator to surface root cause)."
    limited_port: []

deliverables:
  port:
    - deliverable_id: __run__
      description: "Run delivery step."
      outputs:
        - delivery_receipt_json
        - delivery_log_json
        - report_json
    - deliverable_id: delivery_receipt
      description: "Delivery receipt (tenant-visible)."
      outputs:
        - delivery_receipt_json
    - deliverable_id: delivery_log
      description: "Delivery detailed log (tenant-visible)."
      outputs:
        - delivery_log_json
  limited_port: []
