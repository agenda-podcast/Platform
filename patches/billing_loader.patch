*** a/platform/billing/__init__.py
--- b/platform/billing/__init__.py
@@
+from pathlib import Path
+
+def load_module_prices_from_repo(repo_root: Path) -> list[dict]:
+    prices_path = repo_root / "platform" / "billing" / "module_prices.csv"
+    return load_module_prices_from_path(prices_path)
+
+def load_module_prices_from_path(prices_path: Path) -> list[dict]:
+    # existing CSV reading logic, but parameterized
+    ...
